name: mgmess
description: "MGMess — Mattermost client for MyGames"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7

  # Networking
  dio: ^5.8.0
  web_socket_channel: ^3.0.2
  connectivity_plus: ^6.1.4

  # Navigation
  go_router: ^15.1.2

  # Deep Links / OAuth
  app_links: ^6.4.0
  url_launcher: ^6.3.1

  # Storage
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.5.3
  drift: ^2.25.0
  sqlite3_flutter_libs: ^0.5.30

  # Dependency Injection
  get_it: ^8.0.3
  injectable: ^2.5.0

  # Serialization
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

  # Functional
  dartz: ^0.10.1

  # UI
  cached_network_image: ^3.4.1
  flutter_markdown: ^0.7.7
  photo_view: ^0.15.0
  image_picker: ^1.1.2
  file_picker: ^9.2.1

  # Push Notifications
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.0
  flutter_local_notifications: ^18.0.0
  flutter_app_badger: ^1.5.0

  # Utils
  intl: ^0.20.2
  path_provider: ^2.1.5
  mime: ^2.0.0
  path: ^1.9.1
  collection: ^1.19.1
  logger: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.4.15
  freezed: ^3.0.0
  json_serializable: ^6.9.4
  injectable_generator: ^2.7.0
  drift_dev: ^2.25.0

  # Testing
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
